[plugin]

name="Create Actor"
description="Injects a 'Create Actor' context option, which automatically creates a folder, scene, and script, given a name."
author="SirLich"
version="1.0"
script="plugin.gd"
